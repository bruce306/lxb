#---------------------------------------------------
# File: Makefile for tst
#
#    This file is generated by the program lxb.
#
#---------------------------------------------------

APPNAME = tst
UTILNAME = lxbApp_util
PROGRAM = $(APPNAME)

DEFINES = 
CDEBUGFLAGS = -g
INCLUDES = -I.
CFLAGS = $(CDEBUGFLAGS) $(INCLUDES) $(DEFINES)

XVERSION = X11R6

XLIBS = -L/usr/X11R6/lib -lXm -lXpm -lXt -lXext -lX11
LDFLAGS = -o

OBJS = 	$(APPNAME).o $(UTILNAME).o text_object.o

$(PROGRAM):	$(OBJS)
	cc $(LDFLAGS) $(APPNAME) $(OBJS) $(XLIBS)

text_object.o:	text_object.c text_object.h
	cc -c $(CFLAGS) text_object.c

$(APPNAME).o:	$(APPNAME).c	$(APPNAME).h
	cc -c $(CFLAGS) $(APPNAME).c

$(UTILNAME).o:	$(UTILNAME).c	$(UTILNAME).h
	cc -c $(CFLAGS) $(UTILNAME).c

clean:
	rm -f *.o $(APPNAME)

